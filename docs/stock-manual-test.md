# 주식 모의투자 — 수동 테스트 체크리스트

Discord 일반 텍스트 채널에서 테스트합니다.

Soundroom 전용 채널은 서버 설정에 따라 일부 슬래시가 제한될 수 있습니다.

발견한 버그는 코드 수정 전 Known issues에만 적어 둡니다.

슬래시 명령은 클라이언트 입력 그대로 기록합니다.

재현이 어려운 경우 환경 변수와 봇 로그 시점을 함께 적습니다.

## 테스트 환경

- [ ] Node.js 20+
- [ ] .env 설정 완료
- [ ] npm run build 성공
- [ ] npm run start:bot 성공

## 도움말 테스트

- [ ] `/도움말` 실행 시 전체 도움말 표시
- [ ] `/도움말 카테고리:코인` 표시
- [ ] `/도움말 카테고리:주식` 표시
- [ ] `/도움말 카테고리:게임` 표시
- [ ] `/도움말 카테고리:시즌` 표시
- [ ] `/도움말 카테고리:음악` 표시
- [ ] 도움말에 실제 돈·환전과 무관하다는 안내 표시

## 상태 명령 테스트

- [ ] `/상태` 실행
- [ ] 주식 시세 캐시 상태 표시
- [ ] DB 상태 표시
- [ ] 코인 설정 표시
- [ ] 민감정보가 노출되지 않음

## 시세 갱신 모드 (interval / scheduled-close)

- [ ] `STOCK_PRICE_REFRESH_MODE=interval`에서 시작 시 1회 갱신 후 `STOCK_PRICE_REFRESH_INTERVAL_MS`마다 갱신되는지 확인
- [ ] `STOCK_PRICE_REFRESH_MODE=scheduled-close`에서 봇 시작 시 1회 `refreshAll`(시세 준비) 확인
- [ ] scheduled-close 모드에서 `Stock market service started: … mode=scheduled-close, times=… KST` 로그 확인
- [ ] 예약 시각(KST 평일)에 `Scheduled stock quote refresh started: HH:mm KST` 로그 확인
- [ ] 잘못된 `STOCK_SCHEDULED_CLOSE_REFRESH_TIMES_KST`(예: `abc`, `99:99`) 입력 시 설정 경고 후 기본 시각(15:31,15:35,15:40,16:00)으로 동작하는지 확인
- [ ] 주말(KST)에는 예약 갱신 로그가 나오지 않음(시작 시 1회 갱신만 있을 수 있음)
- [ ] 예약 분이 아닌 시간에는 `Scheduled stock quote refresh` 없이 불필요한 반복 갱신이 없음(디버그 로그 과다 없음)

## Mock provider 테스트

- [ ] STOCK_PRICE_PROVIDER=mock 설정
- [ ] /주식목록에 mock 시세 표시
- [ ] `/주식목록` 공개 메시지·ANSI 블록·괄호 순서 퍼센트 \| 변동코인
- [ ] /시세 종목:삼성전자 응답 확인

## 시세·종목 입력

- [ ] `/시세`에 지원 종목 입력 시 시세 표시
- [ ] `/시세`에 미지원 종목·오타 입력 시 지원 목록 안내

## Yahoo provider 테스트

- [ ] STOCK_PRICE_PROVIDER=yahoo 설정
- [ ] /주식목록에 가격 표시
- [ ] 실패 시 Known issues에 기록

## 알바 테스트

- [ ] `/알바` 최초 실행 시 코인 지급
- [ ] `/알바` 후 `/자산`에 반영
- [ ] `/알바` 후 `/랭킹`에 반영
- [ ] 쿨다운 중 재실행 시 실패
- [ ] 봇 재시작 후에도 DB 기록 기준 쿨다운 유지
- [ ] 멘션 알림이 가지 않음

## 낚시 테스트

- [ ] `/낚시` 최초 실행 시 결과 표시
- [ ] 보상이 있으면 `/자산`에 반영
- [ ] 보상이 있으면 `/랭킹`에 반영
- [ ] 꽝이면 잔액 변화 없음
- [ ] 쿨다운 중 재실행 시 실패
- [ ] 봇 재시작 후에도 DB 기록 기준 쿨다운 유지
- [ ] 멘션 알림이 가지 않음

## 일일 미션 테스트

- [ ] `/미션` 최초 조회
- [ ] `/출석` 후 미션 진행도 반영
- [ ] `/알바` 후 미션 진행도 반영
- [ ] `/낚시` 후 미션 진행도 반영
- [ ] `/가위바위보` 후 미션 진행도 반영
- [ ] `/주식목록` 후 미션 진행도 반영
- [ ] 미션 미완료 상태에서 보상받기 실패
- [ ] 미션 완료 후 보상받기 성공
- [ ] 같은 날 보상 중복 수령 실패
- [ ] 봇 재시작 후 미션 진행도 유지

## 코인 상점 테스트

- [ ] `/상점` 목록 표시
- [ ] `/구매 아이템:초보 투자자` 성공
- [ ] 구매 후 잔액 감소
- [ ] 같은 아이템 중복 구매 실패
- [ ] 잔액 부족 시 구매 실패
- [ ] `/내아이템`에 구매한 아이템 표시
- [ ] `/내아이템`은 본인만 보임

## 칭호 장착 테스트

- [ ] `/칭호장착 칭호:초보 투자자` 성공
- [ ] 보유하지 않은 칭호 장착 실패
- [ ] `/내아이템`에 장착 상태 표시
- [ ] `/자산`에 칭호 표시
- [ ] `/랭킹`에 칭호 표시
- [ ] `/칭호해제` 성공
- [ ] 해제 후 `/자산`, `/랭킹`에서 칭호 미표시
- [ ] 멘션 알림이 가지 않음

## 업적 테스트

- [ ] `/업적` 최초 조회
- [ ] `/출석` 후 첫 출석 업적 완료
- [ ] `/알바` 후 알바 업적 완료
- [ ] `/낚시` 후 낚시 업적 완료
- [ ] `/가위바위보` 후 첫 승부 업적 완료
- [ ] `/구매` 후 첫 구매 업적 완료
- [ ] `/칭호장착` 후 칭호 장착 업적 완료
- [ ] `/업적 보상받기:true`로 보상 수령
- [ ] 같은 업적 보상 중복 수령 실패
- [ ] `/프로필`에 업적 진행도 표시

## 프로필 테스트

- [ ] `/프로필` 본인 조회
- [ ] `/프로필 유저:@다른유저` 조회
- [ ] 칭호 장착 시 프로필 제목에 표시
- [ ] 아이템 개수가 표시
- [ ] 최근 게임 기록이 표시
- [ ] 현재 시즌이 표시
- [ ] 업적 진행도(`N / 10`)가 표시
- [ ] 지갑 없는 유저 조회 시 안내
- [ ] 멘션 알림이 가지 않음

## 출석 테스트

- [ ] /출석 최초 실행 시 10,000 코인 지급
- [ ] 같은 날 /출석 재실행 시 중복 지급 없음

## 매수 테스트

- [ ] `/매수` 종목:삼성전자 금액:5000 성공
- [ ] `/주식자산`에 보유 종목 표시
- [ ] `/매수` 지갑 없음 실패(`/출석` 안내)
- [ ] `/매수` 잔액 부족 실패
- [ ] `/매수` 미지원 종목 실패

## 매도 테스트

- [ ] `/매도` 종목:삼성전자 매도:50% 성공
- [ ] `/매도` 종목:삼성전자 매도:전부 성공
- [ ] `/매도` 종목:삼성전자 매도:3000(금액) 성공
- [ ] 보유 없음 실패 케이스 확인
- [ ] 보유 부족 실패 케이스 확인
- [ ] 잘못된 퍼센트 실패 케이스 확인

## 자산 테스트

- [ ] `/자산` 공개 메시지(ephemeral 아님), 총자산만 간단 표시(현금+주식 평가 합)
- [ ] 시세 일부 없을 때 짧은 준비 중 안내
- [ ] `/주식자산`은 본인만 보이는 상세(현금, 평가, 총자산, 누적 입금, 수익률, 보유 목록, 종목명+코드)
- [ ] 보유 없을 때 `보유 종목 없음`

## 랭킹 테스트

- [ ] `/랭킹`이 채널에 공개(ephemeral 아님)·자동 삭제 없음
- [ ] 한 줄 형식(`N위 <@userId> … 코인`), 손익·수익률 미표시
- [ ] `/랭킹`에 총자산 기준 순위 표시
- [ ] 순위 옆에 `<@userId>` 형태로 보이나 멘션 알림은 가지 않음(`allowedMentions`)
- [ ] 참여 전·총자산 0만 있을 때 빈 랭킹 안내 문구 확인
- [ ] 여러 유저 데이터에서 순위 변화 확인

## 관리자 코인 테스트

- [ ] 관리자 권한으로 `/코인지급` 성공
- [ ] 관리자 권한으로 `/코인차감` 성공
- [ ] 일반 유저가 `/코인지급` 사용 시 실패
- [ ] 일반 유저가 `/코인차감` 사용 시 실패
- [ ] 차감 금액이 잔액보다 크면 실패
- [ ] 지급/차감 후 `/자산`에 반영

## 관리자 초기화 테스트

- [ ] 관리자 권한으로 `/유저초기화` 성공
- [ ] 확인 문구가 틀리면 `/유저초기화` 실패
- [ ] 관리자 권한으로 `/서버초기화` 성공
- [ ] 확인 문구가 틀리면 `/서버초기화` 실패
- [ ] 일반 유저는 초기화 명령어 사용 불가
- [ ] 초기화 후 `/자산`, `/랭킹` 결과가 초기화 상태로 반영

## 시즌 테스트

- [ ] 관리자 권한으로 `/시즌시작` 성공
- [ ] 확인 문구가 틀리면 `/시즌시작` 실패
- [ ] ACTIVE 시즌이 있을 때 `/시즌시작` 재실행 실패
- [ ] `/시즌정보`에 현재 시즌 표시
- [ ] `/시즌종료`로 상위 10명 결과 저장
- [ ] `/시즌정보`에 최근 종료 시즌 표시
- [ ] 일반 유저는 `/시즌시작`, `/시즌종료` 사용 불가
- [ ] 시즌 종료 후 자동 초기화되지 않음

## 가위바위보 테스트

- [ ] `/가위바위보` 선택:가위 베팅:100 성공
- [ ] 승리 시 잔액 증가
- [ ] 패배 시 잔액 감소
- [ ] 무승부 시 잔액 유지
- [ ] 잔액 부족 시 실패
- [ ] 지갑 없는 유저는 `/출석` 안내
- [ ] `/가위바위보` 후 `/자산`과 `/랭킹`에 반영
- [ ] 멘션 알림이 가지 않음
- [ ] `/가위바위보` 직후 `/게임기록`에 기록 표시
- [ ] 승리/패배/무승부 결과가 기록에 표시
- [ ] 잔액 변동이 기록에 표시
- [ ] 일반 유저가 다른 유저 기록 조회 시 실패
- [ ] 관리자는 다른 유저 기록 조회 가능
- [ ] `/가위바위보` 연속 시 `/코인설정`의 쿨다운(기본 5초) 안내 확인

## 코인설정 테스트

- [ ] `/코인설정` 조회 성공
- [ ] 관리자 권한으로 출석보상 변경 성공
- [ ] 일반 유저가 설정 변경 시 실패
- [ ] 변경된 출석보상이 `/출석`에 반영
- [ ] 가위바위보 최소/최대 베팅 설정 반영
- [ ] 가위바위보 쿨다운 설정 반영
- [ ] 잘못된 값 입력 시 실패

## Soundroom 채널 정책

- [ ] Soundroom으로 지정된 텍스트 채널에서 주식 슬래시 입력 시 에페메랄 차단 안내 확인

## 봇 재시작 후 DB 유지 테스트

- [ ] 봇 종료
- [ ] 봇 재시작
- [ ] `/자산`으로 기존 지갑과 보유 종목 유지 확인

## Known issues

테스트 중 재현된 버그만 체크한다.

제품 한계는 [stock-investing.md](./stock-investing.md)의 **Known issues (문서화, 코드 미수정)** 섹션을 참고한다.

### 문서화된 한계 (이 단계에서 버그로 다루지 않음)

- KST 기준 공휴일·임시 휴장일은 scheduled-close·평일 판별에 반영하지 않음
- 시세를 DB(`stock_quotes` 등)에 저장하지 않음. 메모리 캐시만 사용
- 봇 재시작 후 시세는 Provider·갱신 정책에 따라 다시 채워짐
- Yahoo provider는 demo/delayed 성격으로 실패·지연 가능
- `/주식목록`·`/시세`의 ANSI 코드블록 색상은 Discord 클라이언트·모바일에 따라 다르게 보일 수 있음

- [ ] 없음

## TODO

- [ ] Known issues 검토 후 수정 여부 결정

---

전체 통과 시 검증 일자와 테스터를 기록합니다.
